metadata:
  type: /type
properties:
  parent: /relation/v1
  template: "{{{ a }}} supervises {{{ b }}}"
  required: ["a", "b"]
  spec:
    a:
      type: string
      pointer_to: /entity/v1/workload
    b:
      type: string
      pointer_to: /entity/v1/workload
